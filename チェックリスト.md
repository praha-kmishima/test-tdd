## 基本機能のテスト

[x] コマンドライン引数からoperationと数値を正しく取得できる
[x] 引数が1個未満の場合エラーを返す
[x] 引数が31個以上の場合エラーを返す
[x] 数値以外の引数が含まれる場合エラーを返す

## 加算（add）のテスト

[x] 2つの正の整数を足して正しい結果を返す
[x] 3つ以上の数値を足して正しい結果を返す
[x] 結果が1000を超える場合は "too big" を返す
[x] 小数点を含む数値の加算が正しく動作する

## 減算（subtract）のテスト

[x] 2つの数値の減算が正しく動作する
[x] 3つ以上の数値の連続した減算が正しく動作する
[x] 結果が負の数になる場合は "negative number" を返す
[x] 小数点を含む数値の減算が正しく動作する

## 乗算（multiply）のテスト

[x] 2つの数値の乗算が正しく動作する
[x] 3つ以上の数値の乗算が正しく動作する
[x] 0を含む数値の乗算が正しく動作する
[x] 結果が1000を超える場合は "big big number" を返す
[x] 小数点を含む数値の乗算が正しく動作する

## 除算（divide）のテスト

[x] 2つの数値の除算が正しく動作する
[x] 0による除算でエラーを返す
[ ] 小数点を含む数値の除算が正しく動作する
